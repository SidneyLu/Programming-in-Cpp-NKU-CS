// 一个很不一样的C++20风格Hello World！
// 记得打开Visual Studio的新特性支持，点击顶部菜单栏"项目"->"属性"页中的“常规”->“C++语言标准”，选择“ISO C++20 标准（/std:c++20）”。
// 或者右键点击解决方案资源管理器中的“项目”（Project），选择“属性”页中的“C/C++”->“语言”->“C++语言标准”，选择“ISO C++20 标准（/std:c++20）”。

import <iostream>;

int main() {
	std::cout << "Hello, C++20!" << std::endl;
	return 0;
}